# Deploy lexmora-api to Irancell-T3 (HAProxy)

**Timestamp:** 2026-07-24 ~17:00–17:15 UTC  
**Host:** t3-new (`cloud-admin@2.144.27.74`, SSH alias `t3`)

## Goal

Check `.armin/docker-scripts/run-on-docker-server.ps1` and deploy lexmora for `lexmora-api.xaigrok.ir` behind HAProxy.

## Commands / steps

1. Inspected HAProxy config under `/cloud-admin/docker-volumes/haproxy/config/haproxy.cfg` — backends already present for `lexmora-api.xaigrok.ir` → `lexmora-api:8080` and cert `lexmora-api.xaigrok.ir.pem`.
2. Fixed YAML: `ssh: "ssh t3"`, `delete_image: "yes"`, moved inline comment off `build_image_on` (flat YAML parser).
3. Fixed script: load image **after** `delete_image` teardown (local build path).
4. Ran `.\.armin\docker-scripts\run-on-docker-server.ps1` — API image built/uploaded; `postgres:16` pull failed (Docker Hub TLS timeout).
5. Manually saved/SCP/`docker load` of local `postgres:16`, then `compose up -d`.

## Important outputs

- Containers: `lexmora-pgsql` (healthy), `lexmora-api` (Up), both on `t3-net` with `haproxy`.
- Health via HAProxy (SNI to 127.0.0.1 / public IP `2.144.27.74`): `GET /api/v1/health` → `{"status":"ok"}` HTTP 200.
- Public DNS: `lexmora.xaigrok.ir` → `2.144.27.74`; **`lexmora-api.xaigrok.ir` has no public A record** (NXDOMAIN via 1.1.1.1).

## Result

Deploy succeeded. HAProxy routing works when Host/SNI is forced to `2.144.27.74`. Browser access by hostname needs a DNS A record for `lexmora-api.xaigrok.ir` → `2.144.27.74`.

## Warnings

- Empty `API_PUBLISH_PORT` still produced an ephemeral host publish (`32769→8080`); HAProxy uses Docker DNS so this is non-blocking.
- Prefer uploading base images when the server cannot reach Docker Hub reliably.
