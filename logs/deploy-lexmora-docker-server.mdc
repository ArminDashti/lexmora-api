# Deploy lexmora-api via run-on-docker-server

- **Timestamp:** 2026-07-26 17:45:43
- **Host:** t3 (2.144.27.74) via `ssh t3 -p 80`
- **Stack dir:** /cloud-admin/docker/lexmora
- **Network:** t3-net (HAProxy → lexmora-api:8080)
- **Public URL:** https://lexmora-api.xaigrok.ir

## Commands

- Updated `.armin/docker-scripts/run-on-docker-server.ps1` for `ssh <alias> -p <port>` (scp uses `-P`)
- Set YAML `ssh: "ssh t3 -p 80"`, empty `publish_port`
- Removed host `ports` from base compose (expose-only for HAProxy)
- Ran `.\.armin\docker-scripts\run-on-docker-server.ps1` successfully

## Result

- Stack up: lexmora-pgsql + lexmora-api on t3-net without host publish
- HAProxy backend `be_lexmora_api` → `lexmora-api:8080`
